{
  "name": "promo-gestao",
  "version": "1.0.0",
  "description": "Aplicativo de Gestão de Promotores",
  "private": true,
  "workspaces": [
    "mobile",
    "backend",
    "web"
  ],
  "scripts": {
    "dev:mobile": "cd mobile && npm start",
    "dev:backend": "cd backend && npm run dev",
    "dev:web": "cd web && npm run dev",
    "install:all": "npm install && cd mobile && npm install && cd ../backend && npm install && cd ../web && npm install"
  }
}

