# URL base da API (produção: Cloud Run; desenvolvimento: seu IP local ou backend URL).
# Em builds de produção (EAS), defina EXPO_PUBLIC_API_URL com a URL do backend.
# Exemplo produção: https://sua-api.run.app/api
EXPO_PUBLIC_API_URL=
